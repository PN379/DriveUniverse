<UserControl x:Class="DriveUniverse.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="18,10,18,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="RULES" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBright}"/>
            <TextBlock Text="  /  automate" FontSize="12" Foreground="{StaticResource TextMuted}" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource GlassCard}" Margin="0,12,0,0" Padding="14,10">
            <DockPanel>
                <Button x:Name="BtnToggle" Style="{StaticResource PrimaryButton}" Content="Start engine" DockPanel.Dock="Right" Padding="12,6" Click="Toggle_Click"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Engine: " Style="{StaticResource LabelMuted}"/>
                    <TextBlock x:Name="EngineState" Text="stopped" Style="{StaticResource LabelMuted}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="Stack"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
